//boolean
//true = Verdadeiro (Os valores são iguais)
//false = Falso (Os valores são diferentes)

const primeiroNumero = 5;
const segundoNumero = 5;

console.log(primeiroNumero === segundoNumero); //Realizamos a comparação dos dois números === (Identico)


const textoUm = "Alura";
const textoDois = 'alura';

console.log(textoUm === textoDois); //Devido a comparação são diferentes pois um está como maiusculo e outrro minusculo