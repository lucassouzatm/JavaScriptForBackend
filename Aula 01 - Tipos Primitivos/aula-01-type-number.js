// tipo number

const meuNumero = 3; //declarei a váriavel e armazenei dentro dela o número 3

const primeiroNumero = 1; //Armazenamos o Primeiro Número
const segundoNumero = 2; //Armazenamos o Segundo Número

const operacaoMatematica = primeiroNumero + segundoNumero; //Declarado nova váriavel e dentro dela realizamos as operações matemáticas

console.log(operacaoMatematica); //Exibimos na tela o resultado da operação matemática

//ponto flutuante

const numeroPontoFlutuante = 3.3; //Declaramos um número com ponto flutuante
const pontoFlutuanteSemZero = .5; //Declaramos o número agora sem o zero JS também aceita desta forma

const novaOperacao = primeiroNumero / numeroPontoFlutuante; //Operação matemática com os números flutuantes

console.log (novaOperacao); //Exibimos na tela o resultado da operação matemática dos números flutuantes.

//NaN - Not A Number (não é um número)

const alura = "Alura";

console.log (alura * primeiroNumero); //Operação matemática que irá retornar o erro NaN pois 'Alura' é uma string e não um number